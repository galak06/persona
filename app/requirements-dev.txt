# Development & CI dependencies
ruff>=0.4.0
# Upper bound is load-bearing, not caution. Under 9.1.1 the directory
# conftest at tests/lib/engagement/conftest.py silently fails to load when the
# CI job passes its ~116 test files explicitly: every fixture it declares
# vanishes and 12 tests die with "fixture 'ig_environment' not found". The same
# file list on 9.0.3 is 1505 passed. It is not an ordering or a name clash --
# the directory alone, or any small subset, loads the conftest fine; it takes a
# large explicit list to reproduce. Missing fixtures are not a documented 9.1
# behaviour change, so this is pinned below it until that is understood
# upstream. CI installs from this file on every run, which is how it drifted
# onto 9.1.1 while every local venv sat on 9.0.3 and passed.
pytest>=8.0.0,<9.1
pytest-cov>=5.0.0
mypy>=1.10.0

# Stage 0 — production scaffolding
structlog>=24.1.0
python-json-logger>=2.0.7
pre-commit>=3.7.0

# Type stubs (for strict mypy on new lib/ modules)
types-requests>=2.31.0
types-croniter>=3.0.3   # lib/scheduling/cron_due.py (strict mypy override)

# HTTP mocking — used by tests/test_wp_comments.py (was missing, broke CI)
respx>=0.21.0

# Per-test timeout. A hanging test used to run until GitHub's 6h job ceiling
# and be cancelled, which reports as a failure with no traceback; with this
# the job fails in seconds and names the test that blocked.
pytest-timeout>=2.3.0
