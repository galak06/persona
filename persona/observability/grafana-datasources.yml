# Auto-provisions Loki as a Grafana datasource on container start -- no
# manual "Add data source" click-through needed after `docker compose
# --profile logging up -d`.

apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
